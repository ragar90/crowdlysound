<% if item.rocks_for?(@current_musician) %>
	<%= link_to "Doesn't rock anymore", rock_path(item.id, item.class.name, 0), :remote => true %>
<% else %>
	<%= link_to "This rocks!", rock_path(item.id, item.class.name, 1), :remote => true %>
<% end %>